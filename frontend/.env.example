# Sao chép thành .env và chỉnh theo môi trường
# URL backend (bao gồm suffix /api)
VITE_API_BASE_URL=http://localhost:8080/api

# Dev với Vite proxy: có thể dùng /api thay vì URL đầy đủ
# VITE_API_BASE_URL=/api
